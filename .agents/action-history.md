# action-history.md — Log máy (KHÔNG ghi tay)

> File này chỉ được cập nhật bởi git hook (`post-commit`) sau mỗi commit khớp
> quy ước `[TaskID] <wip|done|blocked>: <mô tả>`. Agent và người dùng **không**
> tự thêm dòng vào đây — cần ghi chú diễn giải thì ghi vào `record.md`.

| Timestamp | Commit | TaskID | Status mới |
|---|---|---|---|
